package model.foods;

public class FoodFactoryDouble {

    // Cette classe aura une méthode statique, qui renvoie une liste de tous les types de nourriture
    // Une liste de toutes les foods respawnable
    // Une liste de toutes les foods non respawnable
    // Une food qui représente la death food

    // Et du coup, pour créer un plateau, il faut juste lui passer une Factory de food, et il se débrouille avec ça
    
}
